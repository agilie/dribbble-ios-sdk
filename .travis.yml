language: objective-c
podfile: Podfile
xcode_project: DribbbleSDKExample.xcodeproj # path to your xcodeproj folder
xcode_scheme: DribbbleSDKExample
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example
# install:
# - gem install xcpretty --no-rdoc --no-ri --no-document --quiet
# script:
#- set -o pipefail && xcodebuild test -workspace Example/AGLocationDispatcher.xcworkspace -scheme AGLocationDispatcher-Example -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty -c
#- pod lib lint --quick
